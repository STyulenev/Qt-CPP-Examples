find_library(CPACK_EXAMPLE_LIBRARY cpack-lib
    PATHS "@CMAKE_INSTALL_PREFIX@/lib"
)

find_path(CPACK_EXAMPLE_INCLUDE_DIR cpack-lib.h
    PATHS "@CMAKE_INSTALL_PREFIX@/include/@PROJECT_NAME@" 
)

list(APPEND CPACK_EXAMPLE_INCLUDE_DIR ${CPACK_EXAMPLE_INCLUDE_DIR}/../)
set(CPACK_EXAMPLE_LIBRARIES ${CPACK_EXAMPLE_LIBRARY})
set(CPACK_EXAMPLE_LIBS ${CPACK_EXAMPLE_LIBRARY})
set(CPACK_EXAMPLE_INCLUDE_DIRS ${CPACK_EXAMPLE_INCLUDE_DIR})
